import LoginForm from '../../components/LoginForm/LoginForm';

const LoginPage = () => {
  return (
    <div style={{ padding: 20 }}>
      <h2>Log In</h2>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
